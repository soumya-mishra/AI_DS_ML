Pandas:-

Data structure:-
Series - 1D
Dataframes -  2D
Panel - 3D

Series:-
Only series is size immutable.
Values can be mutable.

Creation of Series:-
pandas.Series( data, index, dtype, copy)

data can be ndarry,list,const,dict etc
index value must be unique, default is 0,1,2 etc
dtype is data type
copy default false.

empty series:-
import pandas as pd
pd.Series()
O/P:-
Series([], dtype: float64)

create series from ndarry:-
import pandas as pd
import numpy as np
data = np.array(['a','b','c','d'])
s = pd.Series(data)
print(s)
O/P:-
0   a
1   b
2   c
3   d
dtype: object

Create Series with own index:-
import pandas as pd
import numpy as np
data = np.array(['a','b','c','d'])
s = pd.Series(data,index=[100,101,102,103])
print( s )
O/P:-
100  a
101  b
102  c
103  d
dtype: object

Create a Series from dict:-
data = {'a' : 0., 'b' : 1., 'c' : 2.}
s = pd.Series(data)
print( s )
O/P:-
a 0.0
b 1.0
c 2.0
dtype: float64

Note:- Dictionary keys are used to construct index.

data = {'a' : 0., 'b' : 1., 'c' : 2.}
s = pd.Series(data,index=['b','c','d','a'])
print(s)
O/P:-
b 1.0
c 2.0
d NaN
a 0.0
dtype: float64

Note:-Index order is persisted and the missing element is filled with NaN(Not a Number) 

Create a Series from Scalar:-
s = pd.Series(5, index=[0, 1, 2, 3])
print( s )
O/P:-
0  5
1  5
2  5
3  5
dtype: int64


Dataframes:-
Heterogeneous data
Size Mutable
Data Mutable

Panel:-
Heterogeneous data
Size Mutable
Data Mutable

